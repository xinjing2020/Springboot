<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xsh.mapper.TypeDao">
    <!--两个id可能会相冲，取别名即可-->
    <resultMap id="type" type="com.xsh.pojo.Type">
        <id property="id" column="tid"/>
        <result property="name" column="name"/>
        <collection property="blogs" ofType="com.xsh.pojo.Blog">
            <id property="id" column="bid"/>
            <result property="title" column="title"/>
            <result property="typeId" column="type_id"/>
        </collection>
    </resultMap>
    <!-- 首页右侧展示type对应的博客数量 -->
    <select id="getBlogType" resultMap="type">
        select t.id tid, t.name, b.id bid, b.title, b.type_id
        from t_type t, t_blog b
        where t.id = b.type_id
    </select>

    <select id="getAllType" resultType="com.xsh.pojo.Type">
        select * from t_type
    </select>
</mapper>